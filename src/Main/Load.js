
class Load {

    constructor(id, busy, cyclesNeeded, A, instrid) {
      this.id = id;
      this.busy = busy;
      this.A = A;
      this.cyclesNeeded = cyclesNeeded;
      this.instrid = instrid;
    }


  }
  export {Load};